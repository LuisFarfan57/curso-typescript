"use strict";
(() => {
    // let avengers: number
    let avengers = 10;
    let villanos = 10;
    // if (avengers > villanos) { // Se queja porque avengers es undefined
    if (avengers > villanos) {
        console.log('Estamos salvados');
    }
    else {
        console.log('Estamos muertos');
    }
})();
